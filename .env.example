# Copia este archivo como .env.local (desarrollo) y configura las mismas
# variables en Vercel → Project Settings → Environment Variables (producción).

# Panel de Supabase → Project Settings → API
SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # "service_role" key — secreta, solo en el servidor
